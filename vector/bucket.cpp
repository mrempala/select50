// Bucket Approach
#include "helper.cpp"
#include <stdlib.h> 
#include <vector>

int main(){
    std::vector<int> numVec;
    std::vector<int> resultVec;
    genNumVec(numVec);

    //Add magic here
    std::vector<int> bucketArr[10000];
    
    for(int i = 0; i<numVec.size(); i++){
        int elem = numVec[i];
        i = 
    
    }
    
    
    
    output50Smallest(numVec, "output/output_quick.txt");

    return 0;
}

